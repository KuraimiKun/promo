import React from 'react';
import { Container } from '@mui/material';

const Admin = () => {
  return (
    <Container maxWidth="lg">
      {/* Add any additional admin-related content if needed */}
    </Container>
  );
};

export default Admin;
